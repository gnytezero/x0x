diff --git a/src/high_level/runtime/dual_stack.rs b/src/high_level/runtime/dual_stack.rs
index ed431bf1..60b971b0 100644
--- a/src/high_level/runtime/dual_stack.rs
+++ b/src/high_level/runtime/dual_stack.rs
@@ -241,8 +241,14 @@ impl AsyncUdpSocket for DualStackSocket {
             })?;

         let dest = Self::convert_dest(transmit.destination, socket_is_v6)?;
-        socket.try_send_to(transmit.contents, dest)?;
-        Ok(())
+        // Wrap the non-blocking send in `try_io(Interest::WRITABLE, …)` so that a
+        // `WouldBlock` return clears tokio's internal writable-readiness flag.
+        // `try_send_to` by itself returns errors "without updating the internal
+        // readiness state" (tokio docs); the next `writable()` then resolves
+        // immediately from stale Ready and `drive_transmit` spins at 100% CPU.
+        socket.try_io(tokio::io::Interest::WRITABLE, || {
+            socket.try_send_to(transmit.contents, dest).map(|_| ())
+        })
     }

     fn poll_recv(
diff --git a/src/high_level/runtime/tokio.rs b/src/high_level/runtime/tokio.rs
index cbf19e41..c810366f 100644
--- a/src/high_level/runtime/tokio.rs
+++ b/src/high_level/runtime/tokio.rs
@@ -83,9 +83,17 @@ impl AsyncUdpSocket for UdpSocket {
     }

     fn try_send(&self, transmit: &quinn_udp::Transmit) -> io::Result<()> {
+        // Wrap the non-blocking send in `try_io(Interest::WRITABLE, …)` so that a
+        // `WouldBlock` return clears tokio's internal writable-readiness flag.
+        // `try_send_to` by itself returns errors "without updating the internal
+        // readiness state" (tokio docs); the next `writable()` then resolves
+        // immediately from stale Ready and `drive_transmit` spins at 100% CPU.
         self.inner
-            .try_send_to(transmit.contents, transmit.destination)?;
-        Ok(())
+            .try_io(tokio::io::Interest::WRITABLE, || {
+                self.inner
+                    .try_send_to(transmit.contents, transmit.destination)
+                    .map(|_| ())
+            })
     }

     fn poll_recv(
